// Completa la funcion
// Obten todos los numeros primos del numero que se pasa como argumento
// Retorna el numero primo mas alto

const higherPrimeNumber = (number) => {

}

export default higherPrimeNumber
